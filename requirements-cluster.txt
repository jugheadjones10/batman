# Requirements for RF-DETR fine-tuning on cluster
# Install with: pip install --user -r requirements-cluster.txt

# Core ML/Vision
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0,<2.0  # numpy 2.x has compatibility issues with rfdetr
pillow>=10.0.0
rfdetr[metrics]>=1.0.0  # RF-DETR with TensorBoard support
supervision>=0.18.0
tensorboard>=2.15.0

# Dataset management
fiftyone>=1.0.0  # For COCO import/export
roboflow>=1.1.0

# Experiment framework (NEW)
hydra-core>=1.3.0
hydra-submitit-launcher>=1.2.0

# Video/Image processing
opencv-python>=4.10.0
scipy>=1.13.0

# Utilities
pyyaml>=6.0.0
loguru>=0.7.0
tqdm>=4.66.0